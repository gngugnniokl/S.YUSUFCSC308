#include "BinarySearchTree.h" 

int main(){
    BinarySearchTree tree; // create an object
    tree.insert(data 63); tree.insert(data:9); tree.insert(data:19);
    tree.insert(data 27); tree.insert(data:18); tree.insert(data:108);
    tree.insert(data 99); tree.insert(data:81);
    return 0;
}